# Additional geospatial dependencies for shapefile mask creation
# Install these packages to use the create_binary_mask_from_shapefile function

geopandas>=0.13.0
rasterio>=1.3.0
shapely>=2.0.0

# Note: These packages have system dependencies that may need to be installed first:
# Ubuntu/Debian: sudo apt-get install gdal-bin libgdal-dev
# Conda: conda install -c conda-forge geopandas rasterio
